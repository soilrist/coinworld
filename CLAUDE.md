# CLAUDE.md

이 저장소에서 작업을 시작하기 전에 **`HANDOFF.md`를 먼저 읽을 것.** 지금까지의 진행 경위,
반드시 지켜야 할 판단 기준, 남은 작업이 정리되어 있다.

가장 자주 위반되는 규칙만 요약:

- 확인되지 않은 사실을 지어내거나 과장하지 않는다. 새 카피는 `docs/CONTENT.md`에 출처와
  검증 등급을 함께 남긴다. 사진/글 등 소재 사용 가능 여부 판단 기준은 `docs/CONTENT.md` §14.
- 클로드 아티팩트 미리보기의 제목은 **"damefarm"** 이다 — 오타로 보여도 "damifarm"으로
  고치지 말 것 (자세한 이유는 `HANDOFF.md` §1 참고). 실제 코드베이스의 영문 브랜드 표기
  (`nameEn: "Dami Farm"`, npm 패키지명 `damifarm`)와는 별개다.
- 응답은 한국어로 작성한다.

프로젝트 구조/실행 방법은 `README.md`, 남은 작업 목록은 `HANDOFF.md` §4 참고.
